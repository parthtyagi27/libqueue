#include "mutex_queue.hpp"
#include <cstddef>
#include <mutex>
#include <optional>

namespace queues {

// template <typename T, class Container>
// bool GlobalMutexQueue<T, Container>::push(const T& object) {
//     std::scoped_lock<std::mutex> lock{mtx};
// }

// /*
//  * * front() returns an optional value to the front of the queue
//  */
// template <typename T, class Container>
// std::optional<T> GlobalMutexQueue<T, Container>::front() {
//     std::scoped_lock<std::mutex> lock{mtx};
// }


}
